# Appendix: JSON

Guide section: https://beej.us/guide/bgnet0/html/

## Topics
- JSON syntax (objects, arrays, strings, numbers, booleans, null)
- Python `json.dumps()` and `json.loads()`
- Pretty-printing with `indent=`
- Encoding/decoding to bytes with `.encode()` / `.decode()`

## Notes
<!-- Write your notes here as you read -->
